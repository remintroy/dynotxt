{
  "name": "dynotxt-common-services",
  "version": "1.0.0",
  "description": "common services server for dynotxt",
  "main": "build/index.js",
  "scripts": {
    "build": "tsc -w", 
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.3",
    "nodemailer": "^6.9.1",
    "typescript": "^5.0.4"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.7"
  }
}
