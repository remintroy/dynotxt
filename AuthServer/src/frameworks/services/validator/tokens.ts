export default class tokenValidator {
  isValidAccessToken = async (token: string) => {
    return token;
  };
  isValidRefreshToken = async (token: string) => {
    return token;
  };
  isValidRefreshTokenExist = async (token: string) => {
    return token;
  };
}
